$('#favorite_link-<%=@band.id%>').html("<%=favorite_text%>");

console.log("hello world ----------------")

<% if @favorite %>

  cardToDelete = document.querySelector(".card-container2[data-star-id='<%= @favorite.id %>']");

  if (cardToDelete) {
    // updated starred_bands count
    document.getElementById("starred_bands_count").innerHTML = "<%= current_user.starred_bands.count %>";
    console.log(cardToDelete);
    cardToDelete.remove();

  }
 document.querySelector("h4.title").innerHTML = "<%= current_user.starred_bands.count %> starred bands";


<% end %>

